<div class="align-items-center justify-content-center row main-wrap bg-transparent rounded-lg {{#if fullWidth}}mx-lg-4 mx-0{{/if}}">
    <div class="table-responsive my-4 shadow-lg">
        <table class="table table-striped table-dark text-light1 mb-0" id="dataTable">
            <thead>
                <tr class="bg-main">
                    <th scope="col">{{statusHeaderLabel}}</th>
                    {{#each header}}
                        <th scope="col">{{this}}</th>
                    {{/each}}
                </tr>
            </thead>
            <tbody>
            </tbody>
        </table>
    </div>
</div>