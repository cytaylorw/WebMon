<div class="align-items-center justify-content-center row main-wrap bg-transparent rounded-lg {{#if fullWidth}}mx-4{{/if}}">
    <table class="col-12 table table-striped table-dark my-4 text-light1" id="dataTable">
        <thead>
            <tr class="bg-main">
                <th scope="col">{{statusHeaderLabel}}</th>
                {{#each header}}
                    <th scope="col">{{this}}</th>
                {{/each}}
            </tr>
        </thead>
        <tbody>
        </tbody>
    </table>
</div>